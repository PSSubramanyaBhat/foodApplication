import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  Image,
  StatusBar,
  TextInput,
  TouchableOpacity,
  Alert,
  FlatList,
  ImageBackground,
  Platform,
} from 'react-native';

const Feedback = () => {
  return (
    <View
      style={{
        // alignSelf: 'center',
        marginTop: 100,
      }}>
      <Text style={{textAlign: 'center'}}>Feedback</Text>
    </View>
  );
};

export default Feedback;
