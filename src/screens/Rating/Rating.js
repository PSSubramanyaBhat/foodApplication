import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  Image,
  StatusBar,
  TextInput,
  TouchableOpacity,
  Alert,
  FlatList,
  ImageBackground,
  Platform,
} from 'react-native';

const Rating = () => {
  return (
    <View
      style={{
        // alignSelf: 'center',
        // alignItems: 'center',
        // justifyContent: 'center',
        marginTop: 100,
      }}>
      <Text style={{textAlign: 'center', color: 'black'}}>Rating</Text>
    </View>
  );
};

export default Rating;
